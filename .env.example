# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL root (superuser) password
POSTGRES_ROOT_PASSWORD=your_secure_root_password

# Application database user password
QUESTIONSHUB_PASSWORD=your_secure_app_password

# PostgreSQL data directory path (for local development)
POSTGRES_DATA_PATH=./postgres_data

# =============================================================================
# Admin User Configuration (Production/Docker Only)
# =============================================================================
# These environment variables are REQUIRED for Docker/Production deployment
# Application will fail to start if these are not set
#
# For LOCAL DEVELOPMENT: Configure in appsettings.Development.json instead
# For PRODUCTION: Set these environment variables (loaded from .env on VPS)

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!

# Note: Change these credentials immediately after first deployment
# Admin password must meet security requirements:
# - Minimum 8 characters
# - At least one digit
# - At least one lowercase letter
# - At least one uppercase letter
# - At least one non-alphanumeric character

# =============================================================================
# Deployment Instructions
# =============================================================================
#
# For VPS Deployment (Production):
# 1. Create ~/.env file on VPS with all the variables above
# 2. Set secure permissions: chmod 600 ~/.env
# 3. The CI/CD pipeline will load these variables and pass them to Docker containers
# 4. Never commit .env file to git (already in .gitignore)
#
# For Local Development:
# 1. Database only: Copy .env.example to .env and update POSTGRES_ROOT_PASSWORD
#    and QUESTIONSHUB_PASSWORD for local database
# 2. Application: Configure admin credentials in appsettings.Development.json:
#    {
#      "AdminCredentials": {
#        "Email": "admin@questionshub.local",
#        "Password": "YourDevPassword123!"
#      }
#    }
# 3. Run database: docker compose up --profile dev
# 4. Run application: dotnet run (from QuestionsHub.Blazor folder)

